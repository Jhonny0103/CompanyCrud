##########################
1. Agregamos nuggets
	- Microsoft.EntityFrameworkCore
	- Microsoft.EntityFrameworkCore.SqlServer
	- Microsoft.EntityFrameworkCore.Tools

2. Estructuramos el proyecto.
	- Models
		- Empoyees
		- Dept
	- Data
		- CompanyContext
	- Controllers
		- EmployeesController
		- DeptController
	- Repositories
		- IRepositoryCompany
		- RepositoryCompany

3. Añadimos la conexion a program.cs
	- 1. Agregamos la conexion con la bbdd en el appsettings.json
		builder.Services.AddDbContext<CompanyContext>(options =>
			options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));
	- 2. Agregamos las inyecciones de dependencias: 
		builder.Services.AddDbContext<CompanyContext>(options => options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));